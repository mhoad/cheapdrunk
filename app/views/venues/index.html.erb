<h1>Venues#index</h1>
<p>Find me in app/views/users/index.html.erb</p>
<ul class="users">
  <% @venues.each do |venue| %>
    <li>
      <%= link_to venue.name, user %> signed up <%= user.created_at.to_date %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
